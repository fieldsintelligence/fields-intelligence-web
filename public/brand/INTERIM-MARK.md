# Brand mark — interim

`fi-monogram-midnight-ledger.png` is the **interim** primary mark (Midnight Ledger: navy blocks + brass diagonal on chalk).

Favicons under `/public` and `app/icon.png` / `app/apple-icon.png` are **not** locked to this PNG as final brand. Marketing is generating Z-forward monogram variants (reads as Z, not A). Swap favicons when Zak picks the final mark.

Retired: `fi-tile.png` (periodic-table Fi tile with 26 / Z.F.).
